import { iconSet as fantasiIcon } from '@/assets/icons/fantasi'
import { iconSet as vidaIcon } from '@/assets/icons/vida'

export const IconSet = {
  fantasi: fantasiIcon,
  vida: vidaIcon,
}

export const COOKIE_KEY = {
  AUTH: '__AUTH',
  GUEST_ID: '__GUEST_ID',
}
